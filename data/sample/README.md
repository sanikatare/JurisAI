# Synthetic Sample Fixtures

This directory holds small, deterministic sample fixtures (50–500 rows) mimicking IEEE-CIS column distributions.
These fixtures allow the test suite (`pytest`) and offline demonstration scripts to run without requiring raw Kaggle CSV downloads.
